[package]
name = "pong"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", features = ["2d", "web", "default_font"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1